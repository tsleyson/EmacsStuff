<%= form_for(@micropost) do |f| %>
  <%= rdner 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_are :content, placeholder: "Compose new micropost..."
  %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end  %>
