;; Write a simple program to open a particular file ("~/ekoans.txt")
;; and select a random line from that text file, that is displayed to
;; the user in a new temporary buffer. Call the function ekoan-random,
;; and make it callable by the user.
(defun ekoan-random ()
  (interactive)
  )
